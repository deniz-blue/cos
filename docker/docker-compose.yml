services:
  builder:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: cos-builder
    volumes:
      - ..:/workspace
    # Optional: share host's Gradle cache so dependencies don't redownload
    - gradle-cache:/root/.gradle

volumes:
  gradle-cache:
