# Use hoisted node_modules (flat layout like npm) so that CLI tools like
# @expo/cli and @expo/router-server can resolve project dependencies
# (e.g., expo-router/_ctx-shared) from the project root.
node-linker=hoisted

# Ensure strict peer dependency resolution
strict-peer-dependencies=false
